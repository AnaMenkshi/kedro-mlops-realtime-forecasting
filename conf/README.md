# Configuration

- `parameters` and `catalog` — shared configuration: the data catalog and pipeline parameters. Safe to version control.
-  machine-specific overrides and credentials. Excluded from version control via `.gitignore`.

